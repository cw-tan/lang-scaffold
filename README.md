# lang-scaffold

Modular utilities for plug-and-play LangChain/LangGraph workflows.

## Installation

Install the package in editable mode with pip from the project root.

```bash
pip install -e .
```